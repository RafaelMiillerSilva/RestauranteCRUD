# Sistema Restaurante (Laravel)
## Como rodar:
1. Clone o repositório
2. Rode `composer install`
3. Crie `.env` (baseado em `.env.example`)
4. Rode `php artisan migrate --seed`
5. Inicie o servidor: `php artisan serve`